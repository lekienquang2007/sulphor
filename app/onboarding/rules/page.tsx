// Rules setup — implemented in Phase 2
import { redirect } from "next/navigation"

export default function RulesSetupPage() {
  // Placeholder: Phase 2 implements full rules setup UI
  redirect("/dashboard")
}
